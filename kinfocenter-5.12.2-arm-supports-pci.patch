diff -up kinfocenter-5.14.90/Modules/CMakeLists.txt.0000~ kinfocenter-5.14.90/Modules/CMakeLists.txt
--- kinfocenter-5.14.90/Modules/CMakeLists.txt.0000~	2019-01-21 23:52:30.644209651 +0100
+++ kinfocenter-5.14.90/Modules/CMakeLists.txt	2019-01-22 00:00:31.677251654 +0100
@@ -44,17 +44,12 @@ set_package_properties(PCIUTILS PROPERTI
                        PURPOSE "View PCI details in kinfocenter."
                       )
 
-# PCI module doesn't work on Linux arm64, nor on MacOS, but does on
+# PCI module doesn't work on Linux arm64 (actually it does... Maybe not on Brokenbuntu?),
+# nor on MacOS, but does on
 # FreeBSD arm64, so this is a bit of a tangle of what-is-supported.
 set(_want_pci ON)
 if(APPLE) 
     set(_want_pci OFF)
-elseif(${CMAKE_SYSTEM_PROCESSOR} MATCHES "arm" OR ${CMAKE_SYSTEM_PROCESSOR} MATCHES "aarch64")
-    if (${CMAKE_SYSTEM} MATCHES "FreeBSD")
-        message(STATUS "FreeBSD arm64 pci support enabled.")
-    else()
-        set(_want_pci OFF)
-    endif()
 endif()
 
 if(_want_pci)
